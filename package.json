{
  "name": "@japf-react/root",
  "private": true,
  "scripts": {
    "build": "lerna run build --stream",
    "publish": "lerna publish --registry=https://npm.pkg.github.com/",
    "test": "lerna run test --stream",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "3.4.0",
    "typescript": "4.8.3"
  }
}
